const n1 = 10
const n2 = 20
const add = n1 + n2
console.log(add)

//window-object, document, history, location, screen  these object are not avialable in node js because these are browser object

const arr=[10,20,30,40]
for(e of arr){
    console.log(e)
}

